<?php
/*
|--------------------------------------------------------------------------
| API Routes
|--------------------------------------------------------------------------
|
| Here is where you can register API routes for your application. These
| routes are loaded by the RouteServiceProvider within a group which
| is assigned the "api" middleware group. Enjoy building your API!
|
*/

// Route::middleware('api')->get('/photos', function (Request $request) {
// 	$response = array();
// 	$photos = Photo::all();

// 	foreach($photos as $photo){
// 		$response[] = [
// 			'thumb' => $photo->getMedia('photos')[0]->getUrl('thumb'),
// 			'medium' => $photo->getMedia('photos')[0]->getUrl('medium'),
// 			'large' => $photo->getMedia('photos')[0]->getUrl('large')
// 		];
// 	}

// 	return $response;
// });
